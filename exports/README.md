# Exports

Generated files are produced by:

```bash
python scripts/build_csv.py
```

Expected outputs:

- `all-datasets.csv`
- `all-datasets.json`
- `all-datasets.parquet` when a Parquet engine is installed

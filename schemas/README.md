# Schemas

`dataset.schema.json` is the canonical schema for files under `datasets/**/*.yaml`.

`crosswalk.schema.json` is the starter schema for future machine-readable crosswalk metadata.

Run:

```bash
python scripts/validate.py
```
